apiVersion: v1
kind: Service
metadata:
  name: email-mcp-app
  namespace: email-mcp
  labels:
    app: email-mcp-app
spec:
  selector:
    app: email-mcp-app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
